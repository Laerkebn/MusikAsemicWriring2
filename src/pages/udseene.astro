<div class="flex justify-center mb-8">
  <img 
    src="/billeder/logo.svg" 
    alt="Tone Logo"
    class="h-24"
  />
</div>

<!-- Samlet boks -->
<div class="bg-[#FFFFF0] rounded-lg shadow-md p-6 mt-8 w-fit mx-auto">
  <div>
    <h2 class="text-2xl text-[#262626] mb-7 font-bold">Tone → Acemic Writing Generator</h2>

    <div class="flex flex-wrap gap-4"></div>

    <!-- Farve vælger -->
    <div>
      <label class="block font-bold text-sm text-gray-500 mb-2">
        Vælg farve:
      </label>
      <input 
        id="colorPicker"
        type="color"
        value="#1E1E1E"
        class="w-20 h-10 mb-7 rounded cursor-pointer border-2 border-gray-400"
      />
    </div>

    <!-- Styleknapper -->
    <div>
      <label class="block text-sm text-gray-500 font-bold mb-3">
        Vælg tegne-stil:
      </label>
      <div class="flex mb-7 gap-3">
        <button 
          id="roughStyleBtn" 
          class="style-btn bg-[#1768F5] text-lg hover:bg-[#4169b2] text-[#FFFFF0] font-bold py-2 px-4 rounded"
        >
          Rough aesthetic
        </button>

        <button 
          id="softStyleBtn" 
          class="style-btn bg-[#1768F5] text-lg hover:bg-[#4169b2] text-[#FFFFF0] font-bold py-2 px-4 rounded"
        >
          Soft aesthetic
        </button>
      </div>
    </div>

    <!-- Hastigheds Slider -->
    <div>
      <label class="block text-sm text-gray-500 font-bold">
        Hastighed: <span id="speedDisplay">200</span> ms
      </label>
      <input 
        id="intervalMs"
        type="range"
        value="200"
        min="50"
        max="1000"
        step="50"
        class="slider mb-7"  
      />
    </div>

    <!-- Fade Speed Slider -->
    <div>
      <label class="block text-sm text-gray-500 font-bold">
        Fade hastighed: <span id="fadeSpeedDisplay">1.0</span>x
      </label>
      <input 
        id="fadeSpeed"
        type="range"
        value="1.0"
        min="0.1"
        max="100"
        step="0.1"
        class="slider mb-7"  
      />
      <p class="text-xs text-gray-400 -mt-4 mb-7">
        Lavere værdi = kortere levetid & Højere værdi = længere levetid
      </p>
    </div>

    <!-- Fil upload -->
    <div>
      <label class="block text-sm text-gray-500 font-bold mb-2">
        📁 Upload lydfil (Eller start mic...)
      </label>
      <input 
        type="file" 
        id="fileInput" 
        accept="audio/*"
        class="block w-full mb-7 text-sm text-gray-500
               file:mr-4 file:py-2 file:px-4 file:rounded
               file:border-0 file:text-sm file:font-semibold
               file:bg-[#FF7700]  file:text-[#FFFFF0] hover:file:bg-[#bf6e3b]"
      />
    </div>

    <!-- Knapper -->
    <button 
      id="startMic"
      class="bg-[#1768F5] text-lg hover:bg-[#4169b2] text-[#FFFFF0] font-bold py-2 px-4 rounded disabled:bg-gray-300 disabled:cursor-not-allowed">
      ▶ Start mikrofon
    </button>
    <button 
      id="stopMic"
      disabled
      class="bg-[#FF7700] text-lg hover:bg-[#bf6e3b] text-[#FFFFF0] font-bold py-2 px-4 rounded disabled:bg-gray-300 disabled:cursor-not-allowed">
      ⏹ Stop mikrofon
    </button>
  </div>

  <!-- Tone info -->
  <div>
    <h2 class="text-sm text-gray-500 font-bold mt-7 mb-2">Registreret tone</h2>
    <div class="grid grid-cols-2 gap-4">
      <div>
        <p class="text-sm text-gray-400">Tone:</p>
        <p class="text-3xl font-bold text-gray-400" id="detectedNote">—</p>
      </div>
      <div>
        <p class="text-sm text-gray-400">Frekvens:</p>
        <p class="text-3xl font-bold text-gray-400">
          <span id="detectedFreq">—</span> Hz
        </p>
      </div>
    </div>
  </div>
</div>

<!-- A4 hvor SVG ikoner vises -->
<div class="bg-[#FFFFF0] rounded-lg shadow-md p-6 mt-8 w-fit mx-auto">
 

  <!-- Gem A4 som SVG knap -->
  <button 
    id="saveSvg"
    class="bg-[#1768F5] text-lg hover:bg-[#4169b2] text-[#FFFFF0] font-semibold p-2 px-3 rounded m-4">
    💾 Gem som SVG
  </button>

  <!-- A4 fast størrelse - LIGGENDE FORMAT -->
  <div 
    id="paper" 
    class="bg-gray-50 rounded relative overflow-hidden"
    style="width: 1123px; height: 794px; margin: auto; border: 1px solid #ddd;">
    
    <!-- Tekstboks i midten med fleksibel bredde -->
    <div class="absolute inset-0 flex items-center justify-center z-10">
      <div id="textBox" class="inline-block bg-gray-50 rounded-lg px-2">
        <div
          id="centerText"
          contenteditable="true"
          spellcheck="false"
          class="text-center rounded text-6xl mt-3 font-bold leading-none outline-none whitespace-nowrap"
          style="font-family: 'Acumin Pro Condensed', sans-serif;"
        ></div>
      </div>
    </div>

  </div>
</div>

